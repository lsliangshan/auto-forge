# AutoForge

AutoForge 是在受控权限下构建并运行本地工作流的桌面环境。

## Language

**HTTPS URL 模式**：
工作流声明浏览器访问范围的字符串模式；它可以包含主机和路径 glob，但只覆盖 HTTPS URL。运行时实际权限请求仍使用精确 origin。
_Avoid_: 通配 origin、正则权限

**工作流输入解析**：
将当前参数补丁、已有参数、默认值和运行时绑定组合并校验为工作流执行输入的过程。
_Avoid_: 参数猜测、参数生成

**参数收集**：
一次尚未完成、可跨聊天轮次继续的工作流输入解析过程；每个会话最多有一个活动参数收集。
_Avoid_: 工作流提议、待执行工作流

**参数补丁**：
从当前用户消息中提取、用于更新参数收集的部分字段；它不是完整的工作流执行输入。
_Avoid_: 完整参数、最终输入

**工作流执行输入**：
通过工作流原始输入 Schema 严格校验、可以交给工作流运行的最终参数对象。
_Avoid_: 参数补丁、模型参数

**输入待补充**：
工作流意图已经匹配，但执行输入仍缺少、含有错误或需要消歧的参数解析结果。
_Avoid_: 不匹配、无效工作流
