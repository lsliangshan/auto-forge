```
使用 electron + vite.js + vue3 + typescript + tailwind.css + element-plus 开发一个生产级 桌面APP 项目，需要完整的目录结构及文件。
项目主要功能是：
一、聊天
一个聊天界面，用户可以跟内置的AI 大模型聊天，大模型使用 openrouter 。AI可以调用本地已安装的工作流。如果启动了开发者模式，也可以调用开发中的工作流。
如：有一个往目标网页（如：百度）搜索框中填写关键词，并点击搜索按钮的工作流。
用户向AI提问：`使用百度搜索 "今日天气"`时，AI 能根据用户意图，匹配到上述工作流，并执行工作流。

二、工作流管理
工作流，元信息包括：工作流名称、工作流描述、工作流作者、工作流版本号、工作流类别、工作流逻辑代码文件、工作流文件的hash等。
1. 用户可以本地开发、调试工作流脚本，需要在package.json中添加一个
2. 调试完工作流脚本后，可以提交审核
3. 管理员审核通过后，用户的工作流脚本会显示在工作流大厅列表里
4. 用户可以从工作流大厅列表里下载并使用工作流

```

```
准备开发一个桌面APP
项目主要功能是：
一、聊天
一个聊天界面，用户可以跟内置的AI 大模型聊天，大模型使用 openrouter 。AI可以调用本地已安装的工作流。如果启动了开发者模式，也可以调用开发中的工作流。
如：有一个往目标网页（如：百度）搜索框中填写关键词，并点击搜索按钮的工作流。
用户向AI提问：使用百度搜索 "今日天气"时，AI 能根据用户意图，匹配到上述工作流，并执行工作流。
二、工作流管理
工作流，元信息包括：工作流名称、工作流描述、工作流作者、工作流版本号、工作流类别、工作流逻辑代码文件、工作流文件的hash等。
用户可以本地开发、调试工作流脚本

请引导我完善这个项目
```

```
获取网页内容 https://chatgpt.com/share/6a5c7c00-f850-83ea-8945-3b52e38a26c6 ，网络可能加载会有点慢，需要等网页加载完成。
根据网页内容，使用 electron + vite.js + vue3 + typescript + tailwind.css + element-plus 开发一个生产级 桌面APP 项目，需要完整的目录结构及文件。

```

```
重新构建页面整体结构，
1. 聊天页面，主要是聊管理，聊天窗口。
2.
```

```
聊天，同一轮会话，需要添加会话上、下文管理
如果上、下文太长，还需要添加上、下文压缩功能。
```
