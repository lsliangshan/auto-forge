```
参考附件文档，使用 electron + vite.js + vue3 + typescript + tailwind.css 开发一个生产级 桌面APP 项目，需要完整的目录结构及文件。
```

```
按附件要求，封装对外的 Automation SDK 。 并提供详细的使用接入的文档，以便第三方用户封装自动化工具。
```

```
第三方用户，应该怎么开始开发一个自动化工具。请提示完整的开发实践，从零开始。
```

```
1. 添加 `浏览器` 菜单，点击打开一个浏览器集成窗口
1.1 类似于 `chrome`，支持打开多个浏览器窗口
1.2 用户可以输入网页地址，支持返回历史记录

2. 添加 `自动化任务` 菜单，点击打开一个自动化任务管理列表页面，先占位。

3. 需要创建一个本地数据库，使用 sqlite。app启动时，先判断是否有数据库，没有就需要先创建。
```
