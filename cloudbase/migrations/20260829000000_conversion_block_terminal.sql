-- Additive deployment marker: the foundation push function accepts the strict
-- message.conversion_block_terminal operation. Existing rows remain immutable.
-- Deploy the updated foundation function in the same migration release.
SELECT 1;
